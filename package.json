{
  "name": "swiftserve",
  "version": "1.2.0",
  "description": "A http serve app looks like express",
  "main": "build/main.js",
  "types": "build/main.d.ts",
  "scripts": {
    "build": "bun scripts/build.ts",
    "postbuild": "bunx tsc --project tsconfig.build.json",
    "prepublishOnly": "bun run build && bumpp"
  },
  "files": [
    "build/**",
    "package.json"
  ],
  "dependencies": {
    "@total-typescript/ts-reset": "^0.5.1",
    "qs": "^6.11.2"
  },
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^4.2.0",
    "@types/qs": "^6.9.8",
    "bumpp": "9.2.0",
    "bun-types": "1.0.1",
    "prettier": "3.0.3"
  }
}
